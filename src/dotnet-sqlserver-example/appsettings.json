{
  // Sql Server: still run in docker
  // docker-compose: Server=db
  // the app runs in Visual Studio then Server=localhost
  //"ConnectionString": "Server=localhost;Database=master;User Id=sa;Password=Your_password123;",
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*"
}
